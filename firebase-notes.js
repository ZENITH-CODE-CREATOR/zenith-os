// Zenith OS Firebase project notes
// Project ID: zenith-os-3c7e3
// Firebase config is still inside app.js to keep the copied app logic working exactly like the original.
// Future version idea: move the firebaseConfig object here and expose it as window.ZENITH_FIREBASE_CONFIG.
